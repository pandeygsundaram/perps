[workspace]
members = [
    "api-server",
    "matching-engine",
    "libs/redis-client", "libs/shared-types",
]

[workspace.dependencies]

redis = { version = "0.27", features = ["tokio-comp", "streams"] }